claude:
  api_key: "" # Populated via environment variable
  model: "claude-3-sonnet-20240229"

kubernetes:
  # kubeconfig_path: "~/.kube/config" # Supports ~ expansion and environment variables (e.g., "") or leave empty for in-cluster
  # For in-cluster deployments, remove or comment out the kubeconfig_path.
  # The application will automatically use the in-cluster service account.

prometheus:
  url: "http://localhost:9090"

loki:
  url: "http://localhost:3100"

argocd:
  url: "https://argocd.example.com"
  token: "" # Populated via environment variable

github_actions:
  token: "" # Populated via environment variable

argus_monitor:
  database_url: "" # Populated via environment variable
